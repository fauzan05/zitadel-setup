# Include once inside http { } — enables proxy_set_header Connection $connection_upgrade;
# for WebSocket-friendly proxying without forcing "upgrade" on non-WS requests.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
